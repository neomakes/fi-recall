# FI:RECALL 백엔드 환경변수 (server/.env 로 복사 — 절대 커밋 금지)
# Claude API 키 — 서버에만 둔다. 브라우저 번들에 절대 넣지 말 것.
ANTHROPIC_API_KEY=sk-ant-...
# 서버 측 접근 코드 — 클라이언트가 x-firecall-passcode 헤더로 보내는 값과 일치해야.
FIRECALL_PASSCODE=change-me
# 포트(기본 8787)
PORT=8787
